# https://docs.jitpack.io/
# JitPack sets ANDROID_HOME; JDK 17 for AGP 8.7 + Kotlin 2.0.
jdk:
  - openjdk17

install:
  - ./gradlew :testchimp-rum:assembleRelease :testchimp-rum:publishReleasePublicationToMavenLocal --no-daemon --stacktrace -x test
